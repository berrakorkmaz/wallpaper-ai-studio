# Separation audit

## Rebuilt as general open-source modules

- Six-step wallpaper workflow and premium editorial interface
- Seamless and mural project rules
- Automatic internal project naming
- Prompt compiler and physical-size ratio calculation
- Production Master versus Marketing Mockup separation
- Collection, mood, scale, six mockup roles, listing preview and local ZIP export
- Adapter contracts, demo implementations, ownership helpers and multi-user schema

## Deliberately excluded

- Existing connector source history
- Any existing Etsy OAuth implementation, callback URL or credentials
- Shop names, shop IDs, user IDs and account data
- Existing environment files, logs, caches, build output and private uploads
- Existing Vercel project configuration and deployment links
- Marketplace auto-publish behavior

The new repository starts with a clean Git history and uses synthetic demo content only.
